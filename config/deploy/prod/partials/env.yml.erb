- name: DATABASE_URL
  # This is an env var that will be used in my app, so I define it here
  valueFrom:
    secretKeyRef:
      name: web # secrets.ejson creates and maintains the `web` Secret
      key: database_url
- name: NON_SECRET_VAR
  # This one isn't secret, so no need to encrypt and fetch it from a k8s Secret
  value: Hello
